 here i mentioned all parts

 for images i used multer first of all install and then require and used

 i paste teh code below which i used for images but now i deleted these part from app. js (i done for practice so that why i dell)
 now i am going to create with follow industry standards

 const multer = require("multer")

 const storage = multer.diskStorage({
    destination: function (req, file, cb) {
      cb(null, './public/images/uploads')
    },
    filename: function (req, file, cb) {
     crypto.randomBytes(12,function (err,bytes){
        const fn=bytes.toString("hex") + path.extname(file.originalname)
        cb(null, fn)
     })
    }
  })
  
  const upload = multer({ storage: storage })

  // multer
app.get('/test', (req,res)=>{
    res.render("test");
});
app.post("/upload",upload.single("image"),(req,res)=>{

})
